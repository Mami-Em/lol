// wait for all content to be loaded
document.addEventListener('DOMContentLoaded', () => {

    // create account
    // document.querySelector('#create_account', onsubmit = () => {

    //     // send request
    //     const request = new XMLHttpRequest;
    //     request.open('POST', '/create_account');

    //     // when request is done
    //     request.onload = () => {

    //         // extract JSON response
    //         const data = JSON.parse(request.responseText);
    //         if (data.success) {
    //             alert('Account created!');
    //         }

    //         // if data failed in the back
    //         else document.querySelector('#create_fail_message').innerHTML = data.data.error;
    //     };
    // });
})